class Position:
    def __init__(self, row, column):
        self.row = row         # Row index of the position (y-coordinate).
        self.column = column   # Column index of the position (x-coordinate).
